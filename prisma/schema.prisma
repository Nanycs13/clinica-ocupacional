generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model ExameMedico {
  id_exame           Int      @id
  empresa            String
  medico_responsavel String
  data_exame         DateTime
  tipo_exame         String
  resultado          String
  afastamento        String
}
